<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    {{>tags}}
</head>
<body>
    <div class="main">
        {{>header}}
        <form action="" class="input">
            <input type="text" id="location" name="location"  placeholder="location (adress, city, region, etc)">
            <button>search</button>
        </form>
        <div class="display__weather">

        </div>
    </div>
    {{>footer}}
    <script src="/js/bundle.js"></script>
</body>
</html>